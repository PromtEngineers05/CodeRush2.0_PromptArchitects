"use strict";

let selectedCitizenComplaintId = null;
let selectedAuthorityComplaintId = null;
let selectedDepartment = "All";
const t = (key, fallback) => window.CivicI18n?.t(key, fallback) || fallback;

function initializeRoleDashboards() { if (document.getElementById("citizen-dashboard-content")) refreshRoleDashboards(); }
function refreshRoleDashboards() { renderCitizenDashboard(); renderAuthorityDashboard(); }
function complaintsNewestFirst() { return CivicComplaintStore.getAll().slice().sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt)); }
function priorityLabel(priority) { return ({ P1: "Critical", P2: "High", P3: "Medium", P4: "Low" })[String(priority || "").split(" ")[0]] || "Medium"; }
function priorityText(item) { return `${item.priority || "P3"} · ${priorityLabel(item.priority)}`; }
function timelineEvent(entry) { return typeof entry === "string" ? { status: entry } : (entry || {}); }

function renderCitizenDashboard() {
    const root = document.getElementById("citizen-dashboard-content"); if (!root) return;
    const session = CivicComplaintStore.getSession();
    if (!session || session.role !== "citizen") { root.innerHTML = dashboardAccessMessage("Citizen", "Sign in as a citizen to view your reports and live status updates."); bindOpenLogin(root); return; }
    const complaints = complaintsNewestFirst().filter(item => item.citizenId === session.userId);
    const active = complaints.find(item => item.id === selectedCitizenComplaintId) || complaints[0];
    if (!active) { root.innerHTML = dashboardAccessMessage("Citizen", "You have no reports yet. Start with an AI-assisted civic issue report.", "#report", t("reportIssue", "Report an issue")); return; }
    const notices = CivicNotificationStore?.getFor(session).filter(notice => notice.complaintId === active.id || notice.type === "resolved").slice(0, 2) || [];
    root.innerHTML = `${renderNotifications(notices, "Citizen update")}<div class="citizen-metrics"><div><span>Submitted</span><strong>${complaints.length}</strong><small>All your reports</small></div><div><span>In progress</span><strong>${complaints.filter(item => !item.status.toLowerCase().includes("resolved")).length}</strong><small>Receiving updates</small></div><div><span>AI verified</span><strong>${complaints.filter(item => item.confidence >= 90).length}</strong><small>High-confidence analysis</small></div></div><div class="citizen-workspace"><article class="role-card citizen-focus-card"><div class="role-card-top"><div><span class="role-kicker">Selected report · ${active.id}</span><h3>${escapeHtml(active.title)}</h3></div><span class="status-pill status-${statusClass(active.status)}">${escapeHtml(active.status)}</span></div><p class="role-location">⌖ ${escapeHtml(active.location)} <span>•</span> ${formatTimestamp(active.createdAt)}</p><div class="citizen-case-meta"><span>${escapeHtml(active.category || active.issue)}</span><span>${escapeHtml(priorityText(active))} · ${escapeHtml(active.severity || "Standard")}</span></div>${renderCitizenUpdate(active)}${renderTimeline(active)}<div class="citizen-card-footer"><span>Expected update: <strong>${escapeHtml(active.eta || "Within 24 hours")}</strong></span><a class="dashboard-link" href="#report">Report another issue →</a></div></article><aside class="role-card citizen-help-card"><span class="role-kicker">AI-assisted reporting</span><h3>Every report has a clear next step.</h3><p>CivicAI attaches the detected category, priority, and responsible department before your report reaches the city team.</p><a href="#report" class="secondary-btn">Start a new report</a></aside></div><div class="role-list-heading"><h3>All your complaints</h3><span>${complaints.length} reports</span></div><div class="citizen-complaint-list">${complaints.map(item => `<button class="citizen-complaint-row" data-citizen-detail="${item.id}"><span class="complaint-icon">${issueSymbol(item.category)}</span><span class="complaint-copy"><strong>${escapeHtml(item.title)}</strong><small>${item.id} · ${escapeHtml(item.category || item.issue)} · ${escapeHtml(priorityText(item))} · ${formatTimestamp(item.createdAt)}</small></span><span class="status-pill status-${statusClass(item.status)}">${escapeHtml(item.status)}</span><span class="row-arrow">→</span></button>`).join("")}</div>`;
    root.querySelectorAll("[data-citizen-detail]").forEach(button => button.addEventListener("click", () => { selectedCitizenComplaintId = button.dataset.citizenDetail; renderCitizenDashboard(); }));
}

function renderAuthorityDashboard() {
    const root = document.getElementById("authority-dashboard-content"); if (!root) return;
    const session = CivicComplaintStore.getSession();
    if (!session || session.role !== "authority") { root.innerHTML = dashboardAccessMessage("Authority", "Sign in as Authority / Admin to manage the live complaint queue."); bindOpenLogin(root); return; }
    const all = complaintsNewestFirst();
    const departments = ["All", ...Array.from(new Set(all.map(item => item.department || "Other")))];
    if (!departments.includes(selectedDepartment)) selectedDepartment = "All";
    const complaints = selectedDepartment === "All" ? all : all.filter(item => item.department === selectedDepartment);
    const selected = complaints.find(item => item.id === selectedAuthorityComplaintId) || complaints[0] || all[0];
    const notices = CivicNotificationStore?.getFor(session).slice(0, 3) || [];
    root.innerHTML = `${renderNotifications(notices, "New citizen reports")}<div class="authority-metrics"><div><span>Incoming reports</span><strong>${all.length}</strong><small>Persisted civic queue</small></div><div><span>Priority response</span><strong>${all.filter(item => item.priority === "P1").length}</strong><small>Critical cases need attention</small></div><div><span>AI verification</span><strong>${averageConfidence(all)}%</strong><small>Average detection confidence</small></div><div><span>Resolved</span><strong>${all.filter(item => item.status.toLowerCase().includes("resolved")).length}</strong><small>Across all reports</small></div></div><div class="authority-layout"><section class="role-card authority-queue"><div class="role-card-top"><div><span class="role-kicker">Newest first · AI prioritized</span><h3>${t("incomingComplaints", "Incoming complaints")}</h3></div><span class="queue-count">${complaints.length} shown</span></div><div class="authority-filters" aria-label="Filter complaints by department">${departments.map(department => `<button type="button" class="${department === selectedDepartment ? "active" : ""}" data-department="${escapeHtml(department)}">${escapeHtml(department === "All" ? t("allDepartments", "All departments") : department)}</button>`).join("")}</div><div class="authority-complaint-list">${complaints.length ? complaints.map(item => `<button class="authority-complaint-row ${item.priority === "P1" ? "is-priority" : ""}" data-authority-detail="${item.id}"><span class="priority-dot ${String(item.priority || "P3").toLowerCase()}">${escapeHtml(item.priority || "P3")}</span><span class="authority-row-copy"><strong>${escapeHtml(item.title)}</strong><small>${item.id} · ${escapeHtml(item.department || "Other")} · ${escapeHtml(priorityText(item))}</small><small>${escapeHtml(item.citizenName || "Citizen")} · ${formatTimestamp(item.createdAt)}</small></span><span class="status-pill status-${statusClass(item.status)}">${escapeHtml(item.status)}</span></button>`).join("") : `<p class="empty-queue">No complaints in this department.</p>`}</div></section><aside class="role-card authority-detail" id="authority-detail">${selected ? authorityDetail(selected) : ""}</aside></div>`;
    root.querySelectorAll("[data-department]").forEach(button => button.addEventListener("click", () => { selectedDepartment = button.dataset.department; selectedAuthorityComplaintId = null; renderAuthorityDashboard(); }));
    root.querySelectorAll("[data-authority-detail]").forEach(button => button.addEventListener("click", () => { selectedAuthorityComplaintId = button.dataset.authorityDetail; renderAuthorityDashboard(); }));
    bindAuthorityActions(root, selected, session);
}

function authorityDetail(item) {
    const latest = timelineEvent(Array.isArray(item.timeline) ? item.timeline[item.timeline.length - 1] : item.status);
    return `<span class="role-kicker">AI case detail · ${item.id}</span><div class="authority-detail-title"><h3>${escapeHtml(item.title)}</h3><span class="priority-tag ${String(item.priority || "P3").toLowerCase()}">${escapeHtml(priorityText(item))}</span></div><div class="ai-detection-summary"><span>AI Vision</span><strong>${escapeHtml(item.category || item.issue)}</strong><small>${item.confidence}% confidence</small></div><div class="authority-meta"><div><span>Reported by</span><strong>${escapeHtml(item.citizenName || "Citizen")}</strong></div><div><span>Department & priority</span><strong>${escapeHtml(item.department || "Other")} · ${escapeHtml(priorityText(item))}</strong></div><div><span>Location</span><strong>⌖ ${escapeHtml(item.location)}</strong></div><div><span>Submitted</span><strong>${formatTimestamp(item.createdAt)}</strong></div></div>${renderAuthorityJourney(item, latest)}<div class="authority-mini-map"><span class="map-pin">⌖</span><span>Ward-level location verified</span></div>${renderWorkflow(item)}<label class="authority-status-label">Complaint status<select data-status-select>${statusOptions(item.status)}</select></label><div class="authority-actions"><button class="secondary-btn" type="button" data-locate>View on map</button><button class="primary-btn" type="button" data-save-status>Save status</button></div>`;
}
function renderWorkflow(item) { const status = item.status.toLowerCase(); if (status.includes("resolved")) return `<div class="workflow-banner done"><strong>Resolved</strong><span>This case has completed the civic workflow.</span></div>`; const next = status.includes("progress") ? ["Resolved", t("resolve", "Resolve")] : status.includes("accepted") || status.includes("assigned") || status.includes("inspection") ? ["Work in progress", t("workInProgress", "Work in progress")] : ["Accepted", t("accept", "Accept")]; return `<div class="workflow-banner"><span>Case workflow</span><strong>New → Accept → Work in progress → Resolve</strong><button type="button" class="primary-btn" data-workflow-status="${next[0]}">${next[1]}</button></div>`; }
function renderAuthorityJourney(item, latest) { const detail = latest.actorName ? `${latest.status} · ${latest.actorName} · ${formatTimestamp(latest.at)}` : latest.status; const journey = [["Citizen report", `${item.citizenName || "Citizen"} · ${formatTimestamp(item.createdAt)}`], ["AI verified", `${item.category || item.issue} · ${item.confidence}% confidence`], ["Routed", `${item.department} · ${priorityText(item)} priority`], ["Resolution status", detail]]; return `<section class="authority-journey" aria-label="Citizen to resolution journey"><div class="authority-journey-heading"><span>Live case journey</span><small>Citizen → AI → Authority → Resolution</small></div><ol>${journey.map(([label, detail], index) => `<li class="${index === 3 ? "current" : ""}"><i>${index < 3 ? "✓" : "4"}</i><span><strong>${escapeHtml(label)}</strong><small>${escapeHtml(detail)}</small></span></li>`).join("")}</ol></section>`; }
function bindAuthorityActions(root, item, session) { if (!item) return; root.querySelector("[data-locate]")?.addEventListener("click", () => document.getElementById("map")?.scrollIntoView({ behavior: "smooth" })); const update = status => { const updated = CivicComplaintStore.updateStatus(item.id, status, session); if (status.toLowerCase().includes("resolved")) CivicNotificationStore?.create({ recipientRole: "citizen", recipientUserId: updated.citizenId, complaintId: item.id, type: "resolved", message: `${item.id} has been resolved by ${session.name}.` }); CivicUtils?.toast?.(`${item.id} updated to “${status}”`, "success"); refreshRoleDashboards(); }; root.querySelector("[data-save-status]")?.addEventListener("click", () => { const status = root.querySelector("[data-status-select]")?.value; if (status) update(status); }); root.querySelector("[data-workflow-status]")?.addEventListener("click", event => update(event.currentTarget.dataset.workflowStatus)); }
function statusOptions(current) { return ["AI verified · awaiting assignment", "Accepted", "Inspection scheduled", "Assigned to field team", "Work in progress", "Resolved"].map(status => `<option ${status === current ? "selected" : ""}>${status}</option>`).join(""); }
function renderNotifications(notices, label) { if (!notices.length) return ""; return `<section class="role-notifications"><strong>${label} <span>${notices.length}</span></strong><div>${notices.map(notice => `<p>${escapeHtml(notice.message)} <small>${formatTimestamp(notice.createdAt)}</small></p>`).join("")}</div></section>`; }
function dashboardAccessMessage(role, message, link = "", label = "Open demo login") { const action = link ? `<a class="primary-btn" href="${link}">${label}</a>` : `<button class="primary-btn" type="button" data-open-login>${label}</button>`; return `<div class="role-access-card role-card"><span class="role-kicker">${role} workspace</span><h3>Demo access required</h3><p>${message}</p>${action}</div>`; }
function bindOpenLogin(root) { root.querySelector("[data-open-login]")?.addEventListener("click", () => window.CivicDemoAuth?.open()); }
function renderCitizenUpdate(item) { const latest = timelineEvent(Array.isArray(item.timeline) ? item.timeline[item.timeline.length - 1] : null); const hasAuthorityUpdate = Boolean(item.updatedAt); const message = hasAuthorityUpdate ? `${latest.actorName || item.department} updated your case to “${item.status}”.` : `AI verified your report and routed it to ${item.department}.`; return `<div class="citizen-update ${hasAuthorityUpdate ? "authority-update" : "ai-update"}"><span class="citizen-update-icon">${hasAuthorityUpdate ? "✓" : "⌁"}</span><span><strong>${hasAuthorityUpdate ? "Latest authority update" : "AI routing update"}</strong><small>${escapeHtml(message)} · ${hasAuthorityUpdate ? formatTimestamp(item.updatedAt) : "Just now"}</small></span></div>`; }
function renderTimeline(item) { const steps = ["Reported", "AI verified", "Officer assignment", "Resolution update"]; const status = item.status.toLowerCase(); const completed = status.includes("resolved") ? 4 : status.includes("progress") ? 3 : status.includes("accepted") || status.includes("assigned") || status.includes("inspection") ? 3 : 2; return `<div class="citizen-timeline">${steps.map((step, index) => `<div class="${index < completed ? "complete" : ""}"><i>${index < completed ? "✓" : index + 1}</i><span>${step}</span><small>${index === 0 ? formatTimestamp(item.createdAt) : index === 1 ? item.confidence + "% confidence" : index === 2 ? item.status : "Next update"}</small></div>`).join("")}</div>`; }
function statusClass(status) { const value = String(status || "").toLowerCase(); return value.includes("resolved") ? "resolved" : value.includes("accepted") || value.includes("assigned") ? "assigned" : "progress"; }
function averageConfidence(items) { return items.length ? (items.reduce((total, item) => total + Number(item.confidence || 0), 0) / items.length).toFixed(0) : 0; }
function formatTimestamp(value) { const date = new Date(value); return Number.isNaN(date.getTime()) ? "Recently" : date.toLocaleString(window.CivicI18n?.getLanguage?.() === "hi" ? "hi-IN" : "en-IN", { day: "numeric", month: "short", hour: "numeric", minute: "2-digit" }); }
function issueSymbol(category = "") { return category.includes("Water") ? "≈" : category.includes("Street") ? "☼" : "◇"; }
function escapeHtml(value) { const element = document.createElement("div"); element.textContent = value ?? ""; return element.innerHTML; }
window.CivicRoleDashboards = { refresh: refreshRoleDashboards };
document.addEventListener("DOMContentLoaded", initializeRoleDashboards);
